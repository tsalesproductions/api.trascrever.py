name: 🚀 Deploy developer dashboard on push

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: cwp.salescode.dev
          username: app
          password: "L0jad0ec0mmerce*"
          server-dir: api_transcrever/


      
      